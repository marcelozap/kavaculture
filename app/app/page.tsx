export default function HomePage() {
  return (
    <main>
      <h1>Kava Culture Ordering API</h1>
      <p>
        This Next.js app is being scaffolded alongside the static concept site.
        Start with <code>/api/menu</code>.
      </p>
    </main>
  );
}
